package com.forgeessentials.chat.command;

public class CommandMessageReplacement // extends MessageCommand
{
    /*
     * @Override private static int sendMessage(CommandSource p_198538_0_, Collection<ServerPlayerEntity> p_198538_1_, ITextComponent p_198538_2_) { if (args.length < 2) { throw
     * new WrongUsageException("commands.message.usage"); } else { ServerPlayerEntity target = getPlayer(server, sender, args[0]);
     * 
     * if (target == null) { throw new PlayerNotFoundException("commands.generic.player.notFound"); } else if (target == sender) { throw new
     * PlayerNotFoundException("commands.message.sameTarget"); } else { ITextComponent message = getChatComponentFromNthArg(sender, args, 1, !(sender instanceof PlayerEntity));
     * ModuleChat.tell(sender, message, target); } } }
     */
}
