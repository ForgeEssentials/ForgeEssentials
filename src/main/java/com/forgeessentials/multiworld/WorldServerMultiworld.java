package com.forgeessentials.multiworld;

public class WorldServerMultiworld// extends ServerWorld
{
    /*
     * private SimpleTeleporter worldTeleporter;
     * 
     * public WorldServerMultiworld(MinecraftServer mcServer, ISaveHandler saveHandler, WorldInfo info, int dimensionId, ServerWorld worldServer, Profiler profilerIn, Multiworld
     * world) { super(mcServer, saveHandler, info, dimensionId, profilerIn); this.mapStorage = worldServer.getDataStorage(); this.worldScoreboard = worldServer.getScoreboard();
     * this.worldTeleporter = new SimpleTeleporter(this); }
     * 
     * @Override public Teleporter getDefaultTeleporter() { return this.worldTeleporter; }
     * 
     * @Override protected void saveLevel() throws MinecraftException { this.perWorldStorage.saveAllData(); this.saveHandler.saveWorldInfo(this.worldInfo); }
     * 
     * @Override public java.io.File getChunkSaveLocation() { return this.saveHandler.getWorldDirectory(); }
     */
}